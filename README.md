A Simple CRUD using Java, JDBC:
=> Created lib and src folders in vs code in Project Folder
=> In lib added mysql-connector jat file.
=> In src created seperate folder for each purpose including with Main.java
=>Folders Namely: StudentDAO(StudentDao.java), model(Student.java),util(DBConnection.java)
=>In util ->Connecting java code with Mysql data base using JDBC .
=>In mode ->What are the columns to add in your Database table and Constructor details.
=>In DAO all the Logic related part will be there:
In CRUD: addStudent(), getStudent(), updateStudent() ,DeleteStudent()->Logics are added.
=Additional to this Some Exceptions also used
=>In main calling methods with required Arguments is there.
=>In console we will get simple print messages and in data base we see the changes in the data using CRUD.
